请分析以下代码文件的组件类型：

## 文件信息
- **文件路径**: {}
- **文件名**: {}

## 文件内容预览
```
{}
```

## 分析要求
请基于以下因素进行分析：
1. 文件路径和目录结构
2. 文件名称和扩展名
3. 文件内容和代码结构
4. 导入导出的模块
5. 函数和类的定义
6. 业务逻辑的特征

## 输出格式要求
请返回以下JSON格式的分析结果：

```json
{{
  "code_purpose": "entry", // 必须是英文枚举值，如 "entry", "agent" 等，严禁使用中文
  "confidence": 置信度0.0-1.0,
  "reasoning": "简要分析推理过程"
}}
```

### 可选枚举值（必须严格返回以下英文单词之一，严禁返回中文描述）：
- "entry" (项目执行入口，如main、index、app等)
- "agent" (智能Agent)
- "page" (前端UI页面)
- "widget" (前端UI组件)
- "specificfeature" (特定功能模块)
- "model" (数据类型或模型)
- "types" (程序接口定义)
- "tool" (特定场景功能工具代码)
- "util" (通用基础工具函数和类)
- "config" (配置相关)
- "middleware" (中间件)
- "plugin" (插件)
- "router" (路由组件)
- "database" (数据库组件)
- "api" (供外部调用的服务API)
- "controller" (MVC架构中的Controller组件)
- "service" (MVC架构中的Service组件)
- "module" (明确的模块组件)
- "lib" (依赖库)
- "test" (测试组件)
- "doc" (文档组件)
- "other" (其他未归类)

### 置信度评估标准：
- **0.9-1.0**: 完全确定，特征非常明显
- **0.7-0.9**: 比较确定，特征比较明显
- **0.5-0.7**: 一般确定，有一定特征
- **0.3-0.5**: 不太确定，特征不明显
- **0.0-0.3**: 很不确定，无法判断